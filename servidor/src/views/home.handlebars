<h1>Página Principal</h1>

<h1>Nuestros Productos</h1>

{{#each pagination.docs}}
  <h3>{{this.title}}</h3>
  <p>{{this.description}}</p>
  <p>{{this.price}}</p>
{{/each}}

<a href="/?page={{pagination.prevPage}}">{{pagination.prevPage}}</a>
<a href="/?page={{pagination.nextPage}}">{{pagination.nextPage}}</a>

